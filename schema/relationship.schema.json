{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://human-kernel.example/schema/human-kernel-schema-relationship.json",
  "$comment": "Placeholder $id host - update once repo/GitHub Pages URL is finalized. Mirrors Spec v0.1 §3.",
  "title": "Relationship",
  "type": "object",
  "required": ["id", "sourceParameterId", "targetParameterId", "relationshipType", "confidence", "verification"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "sourceParameterId": {
      "type": "string",
      "format": "uuid"
    },
    "targetParameterId": {
      "type": "string",
      "format": "uuid"
    },
    "relationshipType": {
      "type": "string",
      "enum": ["causal", "correlated", "contradicts", "supports"],
      "description": "Closed enum (Spec v0.1 §3) - add new types via a new ADR, not by typing a new string into a note."
    },
    "confidence": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0
    },
    "verification": {
      "type": "string",
      "enum": ["unverified", "user-confirmed", "contradicted"],
      "description": "Defaults to 'unverified' at compile time (Spec v0.1 §5). Only moves to 'user-confirmed' via explicit UI action - ADR-0005, never set by the compiler itself."
    }
  }
}
