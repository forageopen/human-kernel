name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Typecheck (strict mode)
        run: npm run typecheck

      - name: Schema invariant checks (docs/specification-v0.1.md contract)
        run: npm run validate-schema

      - name: Unit tests
        run: npm test
