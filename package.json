{
  "name": "human-kernel",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-native, local-first knowledge environment - Forage DeepMind",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "validate-schema": "node scripts/validate-schema.mjs",
    "build": "tsc",
    "build-sample-data": "node scripts/build-sample-data.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "ajv": "^8.16.0",
    "ajv-formats": "^3.0.1",
    "jsdom": "^24.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
